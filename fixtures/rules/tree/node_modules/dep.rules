# MUST NOT BE DISCOVERED.
#
# `node_modules` is in `SKIP_DIRS`, so the scan never descends here. A vendored
# package that ships an example rules file is not a file the user's imports
# screen should offer to overwrite.

fields date, description, amount
account1 assets:decoy:node-modules
